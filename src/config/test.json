{
  "db": {
    "host": {
      "doc": "Database host name/IP",
      "format": "*",
      "default": "localhost"
    },
    "name": {
      "doc": "Database name",
      "format": "String",
      "default": "test"
    }
  }
}
