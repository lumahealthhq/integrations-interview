{
  "db": {
    "host": {
      "doc": "Database host name/IP",
      "format": "String",
      "default": "localhost"
    },
    "name": {
      "doc": "Database name",
      "format": "String",
      "default": "luma"
    }
  }
}
